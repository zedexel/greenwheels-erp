{
	"private": true,
	"scripts": {
		"postinstall": "cd frontend && yarn install",
		"dev": "cd frontend && yarn dev",
		"build": "cd frontend && yarn build",
		"postbuild": "echo 'Built to greenwheels/public/greenwheels — run: bench --site <site> clear-cache'"
	}
}
